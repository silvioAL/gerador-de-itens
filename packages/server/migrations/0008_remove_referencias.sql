-- "Referências de código" removida (decisão do usuário): pra isso, é melhor
-- o próprio Claude Code com skills/MCP de projeto do usuário, não uma base de
-- conhecimento paralela mantida por esta ferramenta. Some a tabela inteira —
-- linhas seedadas por 0005/0006 vão junto, não há mais rota que as leia.
DROP TABLE IF EXISTS "referencias";
